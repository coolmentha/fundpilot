-- 用户关注的大盘指数列表(secid 格式,逗号分隔,如 "1.000001,1.000300,0.399006")。
-- 单字段而非关联表:单用户场景、列表短(通常 3-8 个指数)、无需对指数做关联查询。
-- null/空表示用默认列表(上证指数 + 沪深300 + 创业板指),由服务层兜底。
ALTER TABLE user_config ADD COLUMN watched_indices VARCHAR(512);
