# 技术设计

## 边界

- 文档层：修正仓位提醒的非拦截语义，并在策略文档中说明 `PENDING_CALIBRATION` 是历史枚举名，不新增迁移。
- 前端层：复用现有 React Router，使用 `lazy`/`Suspense` 拆分页面模块，不改 API。
- 测试层：优先补充现有 Vitest 体系中的用户流程级组件测试，避免引入依赖。

## 兼容性

策略状态数据库值、后端枚举和接口字段保持不变；只改可见文案与说明。动态导入失败沿用应用错误边界/加载状态。
