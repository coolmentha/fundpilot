# v0.8 执行清单

1. 后端信号查询增加关联交易 ID/状态投影。
2. 后端交易视图增加信号原因，供操作确认页展示。
3. 信号列表增加“查看交易”，按状态决定入口。
4. 采纳成功跳转操作确认页并高亮目标交易。
5. 操作确认页支持 `signalId` 定位和信号原因展示。
6. 补充撤销后仍保持 RESPONDED、重复采纳被拒绝的回归测试。

## 验证

- `cd backend; mvn test`
- `cd frontend; npm run lint; npm test; npm run build`
- 覆盖待处理、已回应、已撤销、已确认、已过期和无关联历史信号。
