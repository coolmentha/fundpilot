# 实施计划：基金详情当日分时图

1. 增加分钟估值点模型和同花顺解析，覆盖有效、无效与过期数据。
2. 将合格分钟曲线接入既有实时缓存和只读行情 API，补充缓存/API 测试。
3. 增加前端查询与分时图，调整 `FundMarketTab` 为两个 Tab，覆盖空态和切换测试。
4. 更新行情领域文档，运行后端测试与前端 lint/test/build。

## 验证

- `mvn -B test`（JDK 25）
- `npm run lint`
- `npm test`
- `npm run build`

## 风险与回滚

外部分钟线格式可能变化；解析异常隔离为无分时数据，不影响既有估值。回滚仅撤销本次代码与文档变更，无数据库变更。
