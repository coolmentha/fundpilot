# 执行计划

1. 更新后端 `FundIntradayChart`、同花顺解析器、缓存网关、查询处理器和 HTTP View，透传结构化 `tradingSessions`。
2. 补充解析器、Redis 投影/旧快照兼容和分时服务测试，先运行后端聚焦测试确认红绿变化。
3. 更新 `FundIntradayChart.jsx` 的交易段展开、未来空槽和百分比基准逻辑；同步组件测试覆盖午休、省略未来价格、15:00 末槽和 NAV 切换。
4. 运行 `git diff --check`、后端聚焦测试、`npm test`、`npm run lint`、`npm run build`。
5. 启动前端预览并用 Playwright/截图检查桌面和移动宽度：时间轴有开盘与收盘，未来区域无曲线，百分比末点与基准计算一致。
6. 复核工作区未跟踪的 `frontend/ux-preview.html` 未被修改；完成后等待用户决定是否提交、合并和部署。

## 回滚点

- 若后端契约测试失败，回退接口字段变更，不触碰估值计算。
- 若真实浏览器无法稳定渲染空槽，只保留结构化时段字段并调整前端投影，不引入新依赖。
