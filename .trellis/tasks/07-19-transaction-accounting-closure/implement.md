# v0.7 执行清单

1. 明确待确认交易的当前原因投影：净值未入库、输入缺失、转换关联腿未就绪。
2. 为买入类待确认交易增加可选预计净值/预计份额展示字段；无净值时返回空和明确状态。
3. 在操作确认页增加确认条件、QDII 延迟说明、当前原因和转换关联展示。
4. 在基金详情交易流水保持已确认实际净值、份额、金额、手续费展示，不新增第二个历史页。
5. 补充后端服务/DTO 单元测试和前端契约测试。

## 验证

- `cd backend; mvn test`
- `cd frontend; npm run lint; npm test; npm run build`
- 覆盖普通基金、QDII、净值缺失、数据源失败、买入、卖出和转换交易。
